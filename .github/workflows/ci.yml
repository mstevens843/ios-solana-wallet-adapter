name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  swift:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Xcode Version
        run: xcodebuild -version
      - name: Verify
        run: scripts/verify-local.sh
